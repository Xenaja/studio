# studio

English-language site for Ksenia Telegina — marketing + development in one pair of hands
(landing pages, chatbots, apps). Targeted at an international audience.

- **Live:** https://xenaja.github.io/studio/
- Single-file static site (`index.html`), plus case pages in `cases/` and `privacy.html`.
- Deployed via GitHub Pages from `main`.

The Russian-language site lives in a separate repository (`lp`) with its own pricing.
